{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Frontend Debug",
            "protocol": "inspector",
            "type": "node",
            "request": "attach",
            "port": 9001,
            "address": "localhost",
            "restart": true,
            "localRoot": "${workspaceRoot}",
            "remoteRoot": "/app"
        },
    ]
}
