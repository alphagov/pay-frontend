var should = require('chai').should();
var assert = require('assert');
var cookies  = require(__dirname + '/../../app/utils/views.js');

describe('views helper', function () {

  it('should have secure proxy off in unsecured environment', function () {
    assert.equal(1, 2);
  });


});
